<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Confetti Button</title>
  <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.3/dist/confetti.browser.min.js"></script>
  <style>
    body {
      text-align: center;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
      background: white;
      overflow: hidden;
    }
    button {
      background-color: white;
      color: black;
      border: 2px solid black;
      padding: 12px 20px;
      font-size: 18px;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.2s;
      position: relative;
      z-index: 1;
    }
    button:hover {
      background-color: #f0f0f0;
    }
    canvas {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 0;
    }
  </style>
</head>
<body>

  <h2>Click to Celebrate</h2>
  <button id="celebrateBtn" onclick="celebrate()">1 Year</button>
  <canvas id="confettiCanvas"></canvas>

  <script>
    const canvas = document.getElementById("confettiCanvas");
    const myConfetti = confetti.create(canvas, { resize: true, useWorker: true });

    function celebrate() {
      const btn = document.getElem
